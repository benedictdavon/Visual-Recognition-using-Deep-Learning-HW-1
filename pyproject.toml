[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    ".codex",
    "agent",
    "data",
    "outputs",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"src/__init__.py" = ["F401"]
"src/*/__init__.py" = ["F401"]
